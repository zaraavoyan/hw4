background (0);
ellipseMode (CENTER);
var y = 20;
while (y < 340) {
  
 {
    fill (255);
    noStroke();
    var x = 20;

    while (x < 340) {
    ellipse (x, y, 20);
    x = x + 30;
  }
    y = y + 30;
}
}
